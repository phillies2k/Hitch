/**
* @name <%= name %>
* @version <%= version %>
*
* <%= name %>.<%= module %>Router
*/
define([

  'jquery', 'underscore', 'backbone', 'hitch'

], function($, _, Backbone, Hitch) {

  return Hitch.Router.extend({

    routes: {
      '<%= module %>': 'index'
    },

    initialize: function() {

    },

    index: function() {

    }

  });

});
