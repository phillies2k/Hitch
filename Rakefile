

desc "Build production version"
task :build do |t|
  sh "uglifyjs hitch.js > hitch.min.js"
end

desc "Creates a fresh application boilerplate"
task :create do |t|

  `git clone git@github.com:phillies2k/hitch-boilerplate`

end